@model IEnumerable<OneCToOneCD.Models.Course>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CourseShortName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CourseDetails)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CourseShortName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CourseDetails.CourseCode)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CourseCode">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CourseCode">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CourseCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
